using UnityEngine;

/// <summary>
/// Used in the combat scene to store the combat moves that the move buttons will use
/// </summary>
public class CombatMoveContainer : MonoBehaviour
{
    public CombatMove combatMove;
}
